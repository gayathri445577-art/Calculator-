#loop:
	the loop is a control structure that is used to  execute  a block of code repeatedly
	#control structure:
		loop is a control.structure because of it control the flow orexecution of program.
#two types:
	for loop:
		number of iterations known
	while loop:
		number of iterations depend on decision
	eg- 
i = 1
while i < 6:
  print(i)
  i += 1